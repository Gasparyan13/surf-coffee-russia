import styled from '@common/styled'

const Root = styled.div`
  height: inherit;
`

const LeftSidebar = styled.div`
  margin-top: 50px;
  margin-left: 30px;
`

export const Styled = { Root, LeftSidebar }
