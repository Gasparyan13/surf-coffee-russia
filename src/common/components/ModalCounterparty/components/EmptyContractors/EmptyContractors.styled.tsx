import { css } from '@emotion/react'

import { theme } from '@providers/ThemeProvider/theme'

export const statusModalCSS = css`
  && {
    text-align: center;
    margin: 16px 0;
  }
`
export const svgDocStyleCSS = css`
  && {
    height: 164px;
    margin: 16px 0;
    color: ${theme.colors.asphaltSuperLight};
  }
`
