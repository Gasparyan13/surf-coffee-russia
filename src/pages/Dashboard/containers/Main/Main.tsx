import React from 'react'

import { Layout } from '../../components/Layout'
import { SpotMetricsReport } from '../../components/SpotMetricsReport'

export const Main: React.FC = () => (
  <Layout>
    <SpotMetricsReport />
  </Layout>
)
