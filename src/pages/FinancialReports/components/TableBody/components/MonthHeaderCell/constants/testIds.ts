const TEST_ID_PREFIX = 'month-header-cell'

export const TEST_ID_MONTH_HEADER = `title-${TEST_ID_PREFIX}`
export const TEST_ID_MONTH_HEADER_PLAN = `plan-${TEST_ID_PREFIX}`
export const TEST_ID_MONTH_HEADER_FACT = `fact-${TEST_ID_PREFIX}`
