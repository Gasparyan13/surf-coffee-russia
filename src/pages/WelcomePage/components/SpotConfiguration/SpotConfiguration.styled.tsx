import { css } from '@emotion/react'

import styled from '@common/styled'

export const spotConfigLoaderGridCSS = css`
  && {
    height: 800px;
  }
`

const Root = styled.div`
  height: 100%;
`

export const Styled = { Root }
