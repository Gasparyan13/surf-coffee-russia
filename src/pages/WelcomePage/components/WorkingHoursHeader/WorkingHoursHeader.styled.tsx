import styled from '@common/styled'

const Root = styled.div`
  height: inherit;
`

const HeaderBreadcrumb = styled.div``

const HeaderTitle = styled.div``
export const Styled = { Root, HeaderTitle, HeaderBreadcrumb }
