import styled from '@common/styled'

export const Root = styled.div`
  position: relative;
`

export const ListRow = styled.ul`
  width: 245px;

  list-style: none;
  padding: 0;
  margin: 0;
  overflow: hidden;
`
