app:
  backend: true

  startupProbe:
    path: /health
  readinessProbe:
    path: /health
  resources:
    limits:
      cpu: 500m
      memory: 1Gi
    requests:
      cpu: 250m
      memory: 500Mi
